rule('sample/build/*.o', { 'sample/src/*.c', 'sample/build' }, function(target, match)
  print('Compiling ' .. target .. ' with generic rule...')
  exec('touch ' .. target)
  exec('sleep 1')
  print('Finished compiling ' .. target)
end)
